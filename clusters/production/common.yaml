apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: production
  namespace: flux-system
spec:
  interval: 1m0s
  path: ./manifest/production/namespaces # ini di ambil dari path manifest yang kita buat
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system

---

apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: common
  namespace: flux-system
spec:
  interval: 1m0s
  path: ./manifest/all # ini di ambil dari path manifest yang kita buat
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system